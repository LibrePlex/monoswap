/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  ACCOUNT_HEADER_SIZE,
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { getSwapMarkerSize } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CreateSwapInstructionAccounts = {
  /** Account to pay for the creation of the account */
  payer?: Signer;
  /** Indexing namespace of the marker */
  namespace: Signer;
  /** Authority to transfer incoming asset */
  authority?: Signer;
  /** Escrows the asset and encodes state about the swap */
  swapMarker: PublicKey | Pda;
  /** The asset to be escrowed for the swap */
  incomingAsset: PublicKey | Pda;
  /** External asset connected to the incoming asset */
  externalAsset: PublicKey | Pda;
  /** Group account for the nifty asset, if applicable */
  niftyAssetGroup?: PublicKey | Pda;
  /** ATA account for the swap marker, if applicable */
  swapMarkerAta?: PublicKey | Pda;
  /** ATA account for the authority, if applicable */
  authorityAta?: PublicKey | Pda;
  /** Transfer Program ID of the incoming asset */
  incomingAssetProgram: PublicKey | Pda;
  /** The SPL associated token program account program */
  associatedTokenProgram?: PublicKey | Pda;
  /** System program account */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type CreateSwapInstructionData = {
  discriminator: number;
  incomingAmount: bigint;
  externalAmount: bigint;
};

export type CreateSwapInstructionDataArgs = {
  incomingAmount: number | bigint;
  externalAmount: number | bigint;
};

export function getCreateSwapInstructionDataSerializer(): Serializer<
  CreateSwapInstructionDataArgs,
  CreateSwapInstructionData
> {
  return mapSerializer<
    CreateSwapInstructionDataArgs,
    any,
    CreateSwapInstructionData
  >(
    struct<CreateSwapInstructionData>(
      [
        ['discriminator', u8()],
        ['incomingAmount', u64()],
        ['externalAmount', u64()],
      ],
      { description: 'CreateSwapInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 0 })
  ) as Serializer<CreateSwapInstructionDataArgs, CreateSwapInstructionData>;
}

// Args.
export type CreateSwapInstructionArgs = CreateSwapInstructionDataArgs;

// Instruction.
export function createSwap(
  context: Pick<Context, 'identity' | 'payer' | 'programs'>,
  input: CreateSwapInstructionAccounts & CreateSwapInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'monoswap',
    'MonoRPwMWxcsVEJV27jyEt1f5VoWg3szDBRYUenm221'
  );

  // Accounts.
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    namespace: {
      index: 1,
      isWritable: false as boolean,
      value: input.namespace ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    swapMarker: {
      index: 3,
      isWritable: true as boolean,
      value: input.swapMarker ?? null,
    },
    incomingAsset: {
      index: 4,
      isWritable: true as boolean,
      value: input.incomingAsset ?? null,
    },
    externalAsset: {
      index: 5,
      isWritable: false as boolean,
      value: input.externalAsset ?? null,
    },
    niftyAssetGroup: {
      index: 6,
      isWritable: true as boolean,
      value: input.niftyAssetGroup ?? null,
    },
    swapMarkerAta: {
      index: 7,
      isWritable: true as boolean,
      value: input.swapMarkerAta ?? null,
    },
    authorityAta: {
      index: 8,
      isWritable: true as boolean,
      value: input.authorityAta ?? null,
    },
    incomingAssetProgram: {
      index: 9,
      isWritable: false as boolean,
      value: input.incomingAssetProgram ?? null,
    },
    associatedTokenProgram: {
      index: 10,
      isWritable: false as boolean,
      value: input.associatedTokenProgram ?? null,
    },
    systemProgram: {
      index: 11,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: CreateSwapInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCreateSwapInstructionDataSerializer().serialize(
    resolvedArgs as CreateSwapInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = getSwapMarkerSize() + ACCOUNT_HEADER_SIZE;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
